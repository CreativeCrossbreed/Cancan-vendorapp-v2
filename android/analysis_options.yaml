# Strict analysis options for production-grade Can Can Vendor App
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat these as errors (build-breaking)
    dead_code: error
    missing_return: error
    missing_required_param: error
    # Downgrade these to warnings (common in Supabase-heavy code)
    argument_type_not_assignable: warning
    invalid_assignment: warning
    deprecated_member_use: info
  language:
    # strict-casts disabled: Supabase returns Map<String, dynamic> everywhere.
    # Forcing explicit casts on every map access is impractical.
    strict-casts: false
    strict-raw-types: true

linter:
  rules:
    # === SECURITY ===
    avoid_print: true                       # Use AppLogger instead
    avoid_web_libraries_in_flutter: true

    # === CORRECTNESS ===
    avoid_empty_else: true
    cancel_subscriptions: true
    close_sinks: true
    control_flow_in_finally: true
    empty_statements: true
    hash_and_equals: true
    no_duplicate_case_values: true
    throw_in_finally: true
    unnecessary_statements: true
    valid_regexps: true

    # === STYLE & CONSISTENCY ===
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_single_quotes: true
    unnecessary_brace_in_string_interps: true
    unnecessary_const: true
    unnecessary_new: true
    unnecessary_this: true
    use_rethrow_when_possible: true
    await_only_futures: true
    avoid_dynamic_calls: false       # Supabase maps are dynamic by nature

    # === FLUTTER SPECIFIC ===
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    sort_child_properties_last: true
    use_build_context_synchronously: true
    use_full_hex_values_for_flutter_colors: true

    # === TYPE SAFETY ===
    always_declare_return_types: true
    annotate_overrides: true
    avoid_dynamic_calls: true
    prefer_typing_uninitialized_variables: true
    type_annotate_public_apis: true
